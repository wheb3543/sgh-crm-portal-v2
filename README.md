# بوابة إدارة حملات التسويق - المستشفى السعودي الألماني

## نظرة عامة

بوابة رقمية متكاملة لإدارة حملات التسويق وعلاقات العملاء للمستشفى السعودي الألماني - صنعاء. يوفر النظام حلاً شاملاً لجمع بيانات العملاء المهتمين عبر صفحات هبوط احترافية، وإدارتهم من خلال لوحة تحكم إدارية متطورة (Mini-CRM)، مع تكامل كامل مع Meta Pixel وWhatsApp وإشعارات البريد الإلكتروني.

## المزايا الرئيسية

### صفحات الهبوط الاحترافية
تصميم صفحات هبوط مخصصة لكل حملة إعلانية، مع نماذج تسجيل سهلة الاستخدام تجمع بيانات العملاء المهتمين (الاسم، الهاتف، البريد الإلكتروني).

### لوحة التحكم الإدارية (Mini-CRM)
نظام إدارة علاقات عملاء مصغر يوفر رؤية شاملة لجميع العملاء المسجلين، مع إمكانية تتبع حالة كل عميل (جديد، تم التواصل، تم الحجز، غير مهتم، لم يرد)، والبحث والفلترة المتقدمة.

### تتبع مصدر الحملات
تسجيل تلقائي لمصدر كل عميل عبر UTM parameters، مما يساعد على قياس أداء الحملات الإعلانية بدقة وتحديد القنوات الأكثر فعالية.

### تكامل Meta Pixel و Conversion API
ربط كامل مع Meta Pixel لتتبع جميع الزيارات والتحويلات، مما يساعد على تحسين أداء الإعلانات على Facebook وInstagram وقياس العائد على الاستثمار بدقة.

### إشعارات فورية عبر البريد الإلكتروني
إرسال إشعار فوري عبر البريد الإلكتروني إلى الفريق المختص عند كل تسجيل جديد، مع قالب بريد إلكتروني احترافي يحتوي على جميع تفاصيل العميل.

### تكامل WhatsApp Business API
إرسال رسائل ترحيب تلقائية للعملاء فور التسجيل، ورسائل تأكيد الحجز، مع إمكانية إرسال رسائل مخصصة من لوحة التحكم.

## التقنيات المستخدمة

### Frontend
- **React 19** - مكتبة JavaScript حديثة لبناء واجهات المستخدم
- **TypeScript** - لغة برمجة مكتوبة بشكل ثابت تبني على JavaScript
- **Tailwind CSS 4** - إطار عمل CSS utility-first للتصميم السريع
- **shadcn/ui** - مكتبة مكونات UI قابلة للتخصيص
- **tRPC** - إطار عمل TypeScript-first لبناء APIs آمنة من حيث النوع
- **Wouter** - مكتبة توجيه خفيفة الوزن

### Backend
- **Node.js** - بيئة تشغيل JavaScript من جانب الخادم
- **Express 4** - إطار عمل ويب سريع ومرن
- **tRPC 11** - طبقة API آمنة من حيث النوع
- **Drizzle ORM** - ORM حديث وخفيف الوزن لـ TypeScript

### Database
- **MySQL/TiDB** - قاعدة بيانات علائقية قابلة للتطوير

### Authentication
- **Manus OAuth** - نظام مصادقة آمن ومتكامل

### Integrations
- **Meta Pixel** - تتبع التحويلات والإعلانات
- **WhatsApp Business API** - إرسال الرسائل التلقائية
- **Email Service** - إشعارات البريد الإلكتروني

## البنية التقنية

```
sgh-crm-portal/
├── client/                 # Frontend application
│   ├── public/            # Static assets
│   │   └── assets/        # Images and logos
│   └── src/
│       ├── components/    # Reusable UI components
│       │   ├── ui/       # shadcn/ui components
│       │   └── MetaPixel.tsx
│       ├── pages/        # Page components
│       │   ├── CampaignLanding.tsx
│       │   ├── ThankYou.tsx
│       │   └── AdminDashboard.tsx
│       ├── lib/          # Utilities
│       │   └── trpc.ts
│       ├── App.tsx       # Main app component
│       └── index.css     # Global styles
├── server/               # Backend application
│   ├── _core/           # Core framework files
│   ├── db.ts            # Database queries
│   ├── routers.ts       # tRPC routers
│   ├── email.ts         # Email service
│   └── whatsapp.ts      # WhatsApp service
├── drizzle/             # Database schema
│   └── schema.ts
└── shared/              # Shared types and constants
```

## التثبيت والتشغيل

### المتطلبات
- Node.js 22+
- pnpm
- MySQL/TiDB database

### خطوات التثبيت

1. **استنساخ المشروع**
```bash
cd sgh-crm-portal
```

2. **تثبيت الحزم**
```bash
pnpm install
```

3. **إعداد قاعدة البيانات**
```bash
pnpm db:push
```

4. **تشغيل الخادم**
```bash
pnpm dev
```

5. **فتح المتصفح**
```
http://localhost:3000
```

## التكوين

### متغيرات البيئة

يتم حقن المتغيرات التالية تلقائياً من قبل المنصة:

- `DATABASE_URL` - رابط الاتصال بقاعدة البيانات
- `JWT_SECRET` - مفتاح تشفير الجلسات
- `VITE_APP_ID` - معرف التطبيق
- `OAUTH_SERVER_URL` - رابط خادم OAuth
- `VITE_OAUTH_PORTAL_URL` - رابط بوابة تسجيل الدخول
- `OWNER_OPEN_ID` - معرف المالك
- `OWNER_NAME` - اسم المالك
- `VITE_APP_TITLE` - عنوان التطبيق
- `VITE_APP_LOGO` - شعار التطبيق

### متغيرات إضافية (يجب تكوينها)

- `HOSPITAL_EMAIL` - بريد المستشفى لاستقبال الإشعارات
- `META_PIXEL_ID` - معرف Meta Pixel (يجب تحديثه في الكود)

## الاستخدام

### صفحة الهبوط
- الوصول: `/`
- نموذج تسجيل بسيط وسهل الاستخدام
- تتبع تلقائي للمصدر عبر UTM parameters
- تكامل Meta Pixel للتتبع

### لوحة التحكم الإدارية
- الوصول: `/admin`
- يتطلب تسجيل الدخول
- عرض جميع العملاء المسجلين
- إحصائيات شاملة
- بحث وفلترة متقدمة
- تحديث حالة العملاء
- إرسال رسائل WhatsApp

## قاعدة البيانات

### الجداول الرئيسية

#### users
جدول المستخدمين المصرح لهم بالوصول للنظام.

#### campaigns
جدول الحملات الإعلانية، يحتوي على معلومات كل حملة (الاسم، الفترة، الحالة، إعدادات WhatsApp).

#### leads
جدول العملاء المسجلين، يحتوي على بيانات العميل (الاسم، الهاتف، البريد، الحالة، مصدر الحملة).

#### leadStatusHistory
جدول تاريخ تغييرات حالة العملاء، يسجل كل تحديث مع المستخدم والتاريخ والملاحظات.

## الأمان

- **المصادقة:** نظام OAuth آمن عبر Manus
- **الصلاحيات:** التحكم في الوصول بناءً على الأدوار
- **تشفير البيانات:** جميع البيانات محفوظة بشكل آمن
- **HTTPS:** جميع الاتصالات مشفرة
- **حماية CSRF:** حماية ضد هجمات Cross-Site Request Forgery

## التطوير

### إضافة حملة جديدة

1. إنشاء سجل جديد في جدول `campaigns`
2. تحديد slug فريد للحملة
3. تخصيص رسالة WhatsApp الترحيبية
4. تفعيل/تعطيل WhatsApp حسب الحاجة

### إضافة صفحة هبوط جديدة

1. إنشاء مكون جديد في `client/src/pages/`
2. استخدام نفس نموذج التسجيل مع تغيير `campaignSlug`
3. تخصيص التصميم والمحتوى
4. إضافة المسار في `App.tsx`

### تخصيص رسائل WhatsApp

تعديل الدوال في `server/whatsapp.ts`:
- `sendWelcomeMessage()` - رسالة الترحيب
- `sendBookingConfirmation()` - رسالة تأكيد الحجز
- `sendCustomMessage()` - رسائل مخصصة

## الإنتاج

### قبل الإطلاق

- [ ] تحديث Meta Pixel ID في `client/src/pages/CampaignLanding.tsx`
- [ ] تكوين خدمة البريد الإلكتروني في `server/email.ts`
- [ ] تكوين WhatsApp Business API في `server/whatsapp.ts`
- [ ] تحديث بريد المستشفى في `HOSPITAL_EMAIL`
- [ ] اختبار جميع الوظائف
- [ ] مراجعة الأمان والصلاحيات

### النشر

يتم النشر تلقائياً عبر منصة Manus:
1. حفظ checkpoint من لوحة التحكم
2. النقر على زر "Publish" في واجهة الإدارة
3. سيتم نشر النظام على نطاق `*.manus.space`
4. يمكن ربط نطاق مخصص من إعدادات المشروع

## الدعم

### للمساعدة أو الاستفسارات

- **المستشفى:** المستشفى السعودي الألماني - صنعاء
- **الرقم المجاني:** 8000018
- **الموقع:** صنعاء - شارع الستين الشمالي (بين جولة عمران وجولة الجمنة)

## الترخيص

هذا المشروع مملوك للمستشفى السعودي الألماني - صنعاء. جميع الحقوق محفوظة © 2025.

---

**نرعاكم كأهالينا** 💚
